apiVersion: v1
kind: Service
metadata:
  {{- with .Values.backend }}
  name: "{{ $.Values.app_name }}-{{ .type }}-clusterip"
  namespace: {{ $.Values.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .port }}
      protocol: {{ .protocol }}
  selector:
    app: {{ $.Values.app_name }}
    type: {{ .type }}
  {{- end }}